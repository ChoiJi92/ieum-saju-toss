# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# env (secrets) — 어떤 위치든 차단
.env
.env.*
.env.local
.env.*.local
**/.env
**/.env.*
**/.env.local
**/.env.*.local
.env.example
**/.env.example
.env.qa.example
.env.prod.example

# 인증서·개인키·시크릿 (mTLS, 서명, OAuth 등) — 어떤 디렉토리든 차단
*.pem
*.key
*.crt
*.cer
*.p12
*.pfx
*.der
*.jks
*.keystore
**/.certs/
**/.secrets/
**/secrets/
**/credentials.json
**/service-account*.json

# Vercel 배포 식별자 (project.json 에 projectId/orgId 노출)
.vercel
**/.vercel

# 토스 미니앱 빌드 산출물 (.ait 는 사용자 사주 빌드 가능성 — 절대 push 금지)
build/
ait-build/

# 작업/협업용 계획 문서 (로컬 관리)
docs/plans/*.md

# OS / Editor
Thumbs.db
.AppleDouble
.LSOverride

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.granite
*.ait
.pnp.*
.yarn/
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# ── 정령 이미지·디자인 작업물 (대용량·로컬 전용, git 미추적/미푸시) ──
# 앱이 쓰는 정령 이미지(투명 PNG) — 로컬에서 빌드 시 포함, repo엔 안 올림
/public/spirits/
# 계열별 캐릭터 원본 폴더 (노을쥐/ 새싹용/ 달빛소/ 언덕호랑이/ 이슬…)
/노을*/
/새싹*/
/달빛*/
/언덕*/
/이슬*/
# 루트 디자인 시트 (계열별 캐릭터 HTML/PNG/PDF)
/dalbit-*
/eondeok-*
/iseul-*
/noeul-*
/saessak-*
# 작업 산출물·기획/핸드오프 문서
/pending-*/
/comparisons/
/docs/
/v2 자료/
/SPIRIT_IMAGE_HANDOFF.md
# 검수판·에셋 백업 (로컬 전용)
/spirits-all-*
/asset-backups/
# 에이전트/오케스트레이션 로컬 상태
/.omx/
/.omc/
