{
  "name": "ieum-saju-backend",
  "version": "0.1.0",
  "private": true,
  "description": "이음사주 토스 미니앱 백엔드 — Vercel Serverless Function. 토스 로그인 OAuth2 token exchange + user info.",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "undici": "^6.19.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.0",
    "typescript": "^5.5.0"
  }
}
